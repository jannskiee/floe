# Prior fixes, and the surface each one skipped

| PR                   | Commit                  | What it closed                                                                                                                                                                                                                                                                                                                                                                                  | What it did not look at                                                                                                                                                         |
| -------------------- | ----------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #310                 | 73677d5                 | Go disk path (`safeJoin` -> `sanitizeComponent`, Windows reserved names, trailing dots and spaces); browser display and download attribute (`sanitizeFileName`)                                                                                                                                                                                                                                 | Go terminal (Incoming box, accept prompt, progress bar, error strings); desktop GUI, which shows the same `FirstName` and `FileName` the engine hands it                        |
| #311                 | b0f75e6                 | Browser `normalizeFileSize`: a junk `fileSize` no longer breaks the receiver's progress math                                                                                                                                                                                                                                                                                                    | Go `formatBytes`, which still panicked on a negative or absurd size and took the CLI and the whole desktop process down with it; desktop untouched                              |
| #314                 | beea4ca                 | Browser progress line; Go sanitizer widened to the Unicode bidi controls; `__proto__` rename ordered after sanitizing in `sanitizeZipEntryName`                                                                                                                                                                                                                                                 | Go display sinks (the widened sanitizer was still reachable only through `safeJoin`); desktop untouched again                                                                   |
| 2026-08-28 hardening | fix/peer-display-safety | Go terminal and every engine callback through one `displayText` sharing `sanitizeRune` with the disk sanitizer (so the desktop GUI inherits it); `byteCount` and `maxAnnouncedSize` reject impossible sizes before `formatBytes`; the 1000-byte control cap applies to strings (`controlMsgMax`); `ver` and `reason` cleaned on both sides; browser error banners through `sanitizeDisplayText` | Desktop history entries persisted before this date keep whatever name they held; the browser `onFileStart` callback still receives the raw name (the only consumer discards it) |

Each PR closed the sink it was looking at. The map exists so the next PR looks at all of them.
